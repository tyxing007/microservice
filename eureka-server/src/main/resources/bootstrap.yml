error:
    whitelabel:
      enabled: false

spring:
  application:
    name: eureka-server
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true
      password: c123456
      username: user

eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false
    server:
      waitTimeInMsWhenSyncEmpty: 0